# =============================================================================
# NEXTJS-SUPABASE-CLAUDE STARTER — ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
#
# Setup guide: https://github.com/kevinneuburger/nextjs-supabase-claude-starter#setup
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE
# Get these from: https://supabase.com/dashboard/project/<project-ref>/settings/api
# -----------------------------------------------------------------------------

# Your Supabase project URL (e.g., https://abcdefghij.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=

# Anon/public key — safe to expose in the browser, but Row Level Security
# must be enabled (it is, in this template) to keep data secure.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — NEVER expose in the browser. Used in server-only code
# for admin operations that bypass RLS (e.g., webhook handlers).
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# ANTHROPIC / CLAUDE
# Get this from: https://console.anthropic.com/account/keys
# -----------------------------------------------------------------------------

# Your Anthropic API key
ANTHROPIC_API_KEY=

# Which Claude model to use for chat completions
# Recommended: claude-sonnet-4-20250514 (best balance of quality and cost)
# Options: claude-opus-4-5, claude-haiku-4-5-20251001
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Maximum tokens per response (adjust based on your use case)
ANTHROPIC_MAX_TOKENS=4096

# -----------------------------------------------------------------------------
# STRIPE
# Get these from: https://dashboard.stripe.com/apikeys
# For local development, use test mode keys (prefix: sk_test_, pk_test_)
# -----------------------------------------------------------------------------

# Publishable key — safe to expose in the browser
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Secret key — server-only, never expose in the browser
STRIPE_SECRET_KEY=

# Webhook signing secret — from Stripe dashboard or `stripe listen` CLI output
# Local dev: run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=

# Price IDs from your Stripe dashboard (Products > Prices)
# Create one recurring price for your paid plan
STRIPE_PRO_PRICE_ID=

# -----------------------------------------------------------------------------
# APP CONFIG
# -----------------------------------------------------------------------------

# The public URL of your application (no trailing slash)
# Local development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Rate limiting: max AI chat messages per user per month on free tier
FREE_TIER_MONTHLY_MESSAGE_LIMIT=50

# Rate limiting: max requests per minute per IP for the chat endpoint
CHAT_RATE_LIMIT_PER_MINUTE=10
